---
interface Props {
  message?: string;
}

const { message = '' } = Astro.props;
---

{message && (
  <div class="rounded-lg border border-border bg-muted/50 px-4 py-3 text-sm text-muted-foreground" role="status">
    {message}
  </div>
)}
